// Declarando variáveis
var a;
var name = "Renan";

// NaN (Not a Number)
parseInt("hello", 10); // retorna NaN

// Operadores
var soma = 5 + 3;   // 8
var sub = 10 - 2;   // 8
var mult = 4 * 2;   // 8
var div = 16 / 2;   // 8
